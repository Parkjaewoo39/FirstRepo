using System;

namespace RealPratice
{
    internal class Program
    {
        static void Main(string[] args)
        {
            const  int A = 20;//변수 앞에 const를 붙임과 동시에 초기화
            Console.WriteLine("상수 A의값은?" + A);
            const int a = 30;
            Console.WriteLine("상수 a의값은?" + a);
        }          
        
    }
}